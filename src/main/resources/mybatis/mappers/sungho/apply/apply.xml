<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="apply">
	<resultMap id="applyResult01" type="java.util.Map">
		<result property="c_Id" column="c_Id"  />
		<result property="mem_Id" column="mem_Id" />
		<result property="ap_Date" column="ap_Date" />
		<result property="ap_Yn" column="ap_Yn" />
		<result property="ap_Status" column="ap_Status" />

		
	</resultMap>
	
	<select id="searchList" resultMap="applyResult01" parameterType="applyVO"> 
      <![CDATA[
         select c_Id, mem_Id, ap_Date, ap_Yn, ap_Status
         from apply_mem
       
      ]]>
		<where>
			<if test=" mem_Id != ''  and  mem_Id != null">
				mem_Id = #{mem_Id}
			</if>
		</where>
		  order by ap_Date desc
	</select>
	
	
	<insert id="insertApply" parameterType="java.util.Map">
		insert into apply_mem(c_Id, mem_Id, ap_Date, ap_Yn, ap_Status) 
		values(#{c_Id}, #{mem_Id}, sysdate, #{ap_Yn} , #{ap_Status})
	</insert>	
	
	
	<delete id="deleteApply" parameterType="String">
		delete from co_work where c_Id =#{c_Id}
	</delete>
	
	<update id="updateApply" parameterType="java.util.Map">
		update co_work
		set  c_Name = #{c_Name}
		   , c_Comment = #{c_Comment}
		   , mem_Id = #{mem_Id}
		   , c_Category = #{c_Category}
		where c_Id = #{c_Id}
	</update>
	
	
	<select id="memberCheck" parameterType="String" resultType="int">
      <![CDATA[
		select count(*) from member where mem_Id = #{mem_Id}
		]]>
	</select>

</mapper>