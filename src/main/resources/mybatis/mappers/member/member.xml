<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	<resultMap id="memberResult01" type="MemberVO">
		<result property="mem_Id" column="mem_Id"  />
		<result property="mem_Pwd" column="mem_Pwd" />
		<result property="mem_Name" column="mem_Name" />
	</resultMap>

	<select id="login" resultMap="memberResult01" parameterType="memberVO">
	
	<![CDATA[
		select * from member
		where mem_Id =#{mem_Id} and mem_Pwd=#{mem_Pwd}
	]]>
	
	</select>


	<select id="searchList" resultMap="memberResult01" parameterType="java.util.Map"> 
      <![CDATA[
         select mem_Id, mem_Pwd, mem_Name
         from   test_member
      ]]>
		<where>
			<if test=" mem_Id != ''  and  mem_Id != null">
				id = #{mem_Id}
			</if>
		</where>
	</select>
	
	
	
	
	<insert id="insertMember" parameterType="java.util.Map">
		insert into member(mem_id, mem_pwd, mem_name, mem_joindate, mem_kind) 
		values(#{mem_Id}, #{mem_Pwd}, #{mem_Name}, sysdate, '00')
	</insert>	

</mapper>