<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	<resultMap id="memberResult01" type="MemberVO">
		<result property="memId" column="mem_id"  />
		<result property="memPw" column="mem_pwd" />
		<result property="memName" column="mem_name" />
		<result property="memJoinDate" column="mem_joindate" /> 
	</resultMap>

	<select id="login" resultMap="memberResult01" parameterType="memberVO">
	
	<![CDATA[
		select * from member
		where mem_id =#{memId} and mem_pwd=#{memPw}
	]]>
	
	</select>


	<select id="searchList" resultMap="memberResult01" parameterType="java.util.Map"> 
      <![CDATA[
         select memId, memPw, memName
         from   test_member
      ]]>
		<where>
			<if test=" memId != ''  and  memId != null">
				id = #{memId}
			</if>
		</where>
	</select>
	
	
	
	
	<insert id="insertMember" parameterType="java.util.Map">
		insert into member(mem_id, mem_pwd, mem_name, mem_joindate) values(#{memId}, #{memPw}, #{memName}, #{memJoinDate})
	</insert>	

</mapper>